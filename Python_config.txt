Se necessário rode as seguintes instalações:
python -m pip install --upgrade pip
pip install scipy
pip install scikit-learn
pip install Flask pymongo
pip install statistics
pip install numpy
pip install pandas
pip install pyserial
python -m pip install "pymongo[srv]"